{
  "name": "fully-logistics-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site for Fully Logistics: static HTML, CSS and JavaScript.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=18"
  }
}

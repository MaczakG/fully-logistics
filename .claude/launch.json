{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "fully-logistics-site",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import os; from http.server import ThreadingHTTPServer, SimpleHTTPRequestHandler; ThreadingHTTPServer(('127.0.0.1', int(os.environ.get('PORT', '4173'))), SimpleHTTPRequestHandler).serve_forever()"
      ],
      "port": 4173,
      "autoPort": true
    }
  ]
}
